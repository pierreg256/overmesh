apiVersion: harness.overmesh.io/v1
id: LIST-DELETE-RECREATE-001
suite: listing
environment:
  providers: [model]
seed: 8005
initialState:
  blob: absent
operations:
  - action: putBlob
    blob: /container/recreated
    dataset: tiny/hello.txt
    writeId: list-recreate-1
  - action: listBlobs
    blob: /container/recreated
    maxResults: 1
  - action: deleteBlob
    blob: /container/recreated
    writeId: list-recreate-delete
  - action: listBlobs
    blob: /container/recreated
    maxResults: 1
  - action: putBlob
    blob: /container/recreated
    dataset: tiny/world.txt
    writeId: list-recreate-2
  - action: listBlobs
    blob: /container/recreated
    maxResults: 1
expected:
  clientOutcome:
    class: success
  visibleBlob:
    present: true
    mustNeverBePrepared: true
  healthAfterOperations: HEALTHY
  healthAfterReconciliation: HEALTHY
  currentLogicalVersion: 3
  responses:
    - operation: putBlob
      status: 201
    - operation: listBlobs
      status: 200
    - operation: deleteBlob
      status: 202
    - operation: listBlobs
      status: 200
    - operation: putBlob
      status: 201
    - operation: listBlobs
      status: 200
invariants:
  - INVARIANT-001
  - INVARIANT-009
